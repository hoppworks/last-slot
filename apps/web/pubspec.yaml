name: last_slot
description: An executable reliability case study for a contested booking.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  dio: ^5.7.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.1.0
  google_fonts: ^6.2.1
  go_router: ^17.1.0
  url_launcher: ^6.3.2
  uuid: ^4.5.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  patrol: ^4.8.0
  patrol_cli: ^4.6.1

flutter:
  uses-material-design: true
